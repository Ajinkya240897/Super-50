# Core dependencies (essential)
streamlit>=1.37.0
pandas>=2.1.0
numpy>=1.25.0
yfinance>=0.2.40
requests>=2.31.0
pyarrow>=15.0.0

# Optional performance and stability improvements
tqdm>=4.66.0          # Progress bars for prewarm_cache.py
lxml>=5.2.0           # Faster and more robust XML parsing for Google News RSS
tenacity>=8.2.2       # Retry logic (optional, helps with transient API issues)
joblib>=1.3.0         # Parallel utilities (faster caching/metadata)
certifi>=2024.7.4     # Keeps SSL up-to-date for API calls

# (Optional) text processing and sentiment helpers (if expanded later)
regex>=2023.10.3
